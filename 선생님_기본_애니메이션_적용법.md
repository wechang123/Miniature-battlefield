# 선생님 캐릭터 - 서있는 애니메이션 적용 (초간단)

NavMesh 없이 일단 선생님 캐릭터를 씬에 세우고 Idle 애니메이션만 적용합니다.

## 1단계: 선생님 캐릭터를 씬에 추가

1. **Project 창 (아래쪽) 열기**

2. **폴더 경로 이동:**
   ```
   Assets > scary-teacher > source
   ```

3. **Scary_Teacher-1.fbx 파일 찾기**

4. **Scary_Teacher-1.fbx를 Hierarchy로 드래그**
   - 왼쪽 Hierarchy 창에 드래그하거나
   - 가운데 Scene 뷰에 드래그

5. **Hierarchy에 생성됨:** `Scary_Teacher-1`

## 2단계: 위치 조정

1. **Hierarchy에서 Scary_Teacher-1 선택**

2. **Inspector (오른쪽)에서 Transform 확인**

3. **Position 값 조정:**
   ```
   Position
   X: 0
   Y: 0 (바닥에 맞게 조정, 예: 0.1)
   Z: 0
   ```

4. **Scene 뷰에서 선생님이 바닥 위에 서있는지 확인**
   - 바닥 아래로 떨어져있으면 Y 값을 높이기
   - 공중에 떠있으면 Y 값을 낮추기

## 3단계: Animator 추가 (서있는 애니메이션)

1. **Scary_Teacher-1 오브젝트가 선택된 상태에서**

2. **Inspector 하단 "Add Component" 버튼 클릭**

3. **검색창에 입력:** `Animator`

4. **"Animator" 선택하여 추가**

## 4단계: Animator Controller 연결

Animator 컴포넌트가 추가되면:

1. **Controller 칸에 연결:**
   - Controller 옆의 **⊙ (동그라미) 버튼** 클릭
   - 또는 드롭다운 클릭

2. **검색창에 입력:** `StarterAssetsThirdPerson`

3. **선택:**
   ```
   StarterAssetsThirdPerson
   (Assets/StarterAssets/.../StarterAssetsThirdPerson.controller)
   ```

## 5단계: Avatar 설정 확인

Animator 컴포넌트에서:

1. **Avatar 칸 확인**
   - 자동으로 `Scary_Teacher-1Avatar`가 설정되어 있어야 함
   - 비어있으면 ⊙ 버튼 클릭해서 `Scary_Teacher-1Avatar` 선택

2. **Apply Root Motion 체크 해제**
   - ☐ Apply Root Motion (체크 해제)

## 6단계: 테스트

1. **Play 버튼 (▶) 클릭**

2. **결과 확인:**
   - ✅ 선생님이 씬에 서있음
   - ✅ 숨쉬는 듯한 자연스러운 Idle 애니메이션 재생

3. **문제가 있다면:**
   - 애니메이션이 안 보이면 → Controller가 제대로 연결되었는지 확인
   - 선생님이 안 보이면 → Scene 뷰에서 더블클릭하여 줌인

## 완료!

이제 선생님 캐릭터가 씬에 서있고 자연스러운 Idle 애니메이션이 재생됩니다.

---

## 추가: 여러 선생님 배치

같은 선생님을 여러 개 배치하고 싶다면:

1. **Hierarchy에서 Scary_Teacher-1 선택**
2. **Ctrl+D (복제) 또는 우클릭 > Duplicate**
3. **복제된 오브젝트의 Position 변경** (다른 위치에 배치)

---

## 다음 단계 (선택사항)

나중에 순찰 시스템을 추가하려면:
- NavMesh 베이킹 필요
- AI Navigation 패키지 설치 필요
- TeacherPatrolAI 스크립트 추가

하지만 지금은 일단 서있는 선생님만 배치하면 됩니다!

---

## 요약 - 3단계만 기억!

1. **Scary_Teacher-1.fbx → Hierarchy로 드래그**
2. **Add Component → Animator**
3. **Controller → StarterAssetsThirdPerson 선택**

끝! ✅
